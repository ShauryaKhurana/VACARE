# Copy to .env.local for local Next.js.
#
# Unset / omit this file → mock fixtures, no Python needed.
# http://127.0.0.1:8000 → talk to `python -m src.web` on its own port.
# / → same-origin (used in Docker; Next proxies /api to Python).
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
